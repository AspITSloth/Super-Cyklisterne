{% extends 'base.html.twig' %}

{% block title %}Sign up{% endblock %}

{% block body %}
     
     <div class="col-sm m-4">
        <h5>OPRET BRUGER HER</h5>
        {{ form(form) }}
        {# {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}         #}
    </div>

{% endblock %}